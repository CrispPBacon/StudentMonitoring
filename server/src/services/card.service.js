/* <--- GENERATE STUDENT CARD ID ---> */
export async function generateCard(student_id) {
  return student_id;
}

/* <--- SUSPENDS STUDENT CARD ---> */
export async function suspendCard(card_id) {
  return card_id;
}

/* <--- REACTIVATE STUDENT'S CARD ---> */
export async function reactivateCard(card_id) {
  return card_id;
}

/* <--- PERMANENTLY DELETES STUDENT'S CARD ID ---> */
export async function deleteCard(card_id) {
  return card_id;
}
