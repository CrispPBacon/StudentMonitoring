/* <--- ADD STUDENT ---> 
    NOTE: Register Student to Database
*/
export async function addStudent(first_name, last_name, student_id, course) {
  return (first_name, last_name, student_id, course);
}

/* <--- GET STUDENT INFORMATION ---> 
    NOTE: Fetch Student Information From the Database.
*/
export async function getStudent(student_id) {
  return student_id;
}
